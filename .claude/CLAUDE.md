- Use `agent-browser` for web automation. Run `agent-browser --help` for all commands.
- Pull requests: Always try to use templates provided by the repository.
- Github: Always use the `gh` cli for github operations.
